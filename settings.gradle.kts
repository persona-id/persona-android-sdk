include(":BasicInquirySample")
include(":InlineInquirySample")

rootProject.name = "Persona SDK Demo"

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
